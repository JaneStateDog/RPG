//Draw self but make sure the x and y is the draw ones
if (sprite_index != -1) draw_sprite_ext(sprite_index, round(image_index), round(drawX), round(drawY),
										image_xscale, image_yscale, image_angle, image_blend, image_alpha);